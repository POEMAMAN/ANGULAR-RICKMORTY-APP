import { CharacterspageComponent } from './characterspage.component';

describe('CharacterspageComponent', () => {
  let component: CharacterspageComponent;

  beforeEach(() => {
    component = new CharacterspageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
